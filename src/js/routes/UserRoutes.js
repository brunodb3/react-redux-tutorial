// src/js/routes/UserRoutes.js

// custom modules
import Users from '../components/users';

// exporting the users module routes
export default {
  DEFAULT: {
    title: 'Users',
    pathname: `/users`,
    component: Users,
  },
};
